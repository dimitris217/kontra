<?xml version="1.0" encoding="utf-8"?>
<!-- ============================================================
     ΑΝΤΙΓΡΑΨΕ ΑΥΤΑ ΣΤΟ AndroidManifest.xml ΤΟΥ CAPACITOR PROJECT
     (android/app/src/main/AndroidManifest.xml)

     1. VIBRATE  -> χωρις αυτο, το navigator.vibrate() στην αλλαγη
                    ομιλητη δεν κανει τιποτα μεσα σε WebView.
     2. screenOrientation="portrait" -> κλειδωνει την οθονη native.
                    Το screen.orientation.lock() της σελιδας σκαει
                    σιωπηλα μεσα σε app, γι' αυτο το κανουμε εδω.
     3. FULLSCREEN -> το display:"fullscreen" του manifest.json και
                    το requestFullscreen() αγνοουνται εκτος browser.
                    Θελει: npm i @capacitor/status-bar
                    και στο app: StatusBar.hide()
     4. SERVICE WORKER -> ηδη διορθωμενο στο index.html:
                    if (!window.Capacitor && "serviceWorker" in navigator)
                    Ετσι το GitHub Pages συνεχιζει offline, και το app
                    δεν κρατα παλια cache μετα απο update του APK.
     ============================================================ -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.VIBRATE"/>
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.WAKE_LOCK"/>

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:theme="@style/AppTheme">

    <activity
        android:name=".MainActivity"
        android:exported="true"
        android:launchMode="singleTask"
        android:screenOrientation="portrait"
        android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
        android:theme="@style/AppTheme.NoActionBarLaunch">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

  </application>
</manifest>
